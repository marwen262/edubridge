# Base de données PostgreSQL
DB_NAME=edubridge
DB_USER=postgres
DB_PASS=
DB_HOST=localhost
DB_PORT=5432

# JWT
JWT_SECRET=changeme_secret_fort
JWT_EXPIRES=7d

# SMTP (Nodemailer)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=noreply@edubridge.tn

# Rate limiting (optionnel — valeurs par défaut utilisées si absent)
# RATE_LIMIT_DISABLED=false
# RATE_LIMIT_GLOBAL_MAX=600
# RATE_LIMIT_LOGIN_MAX=20
# RATE_LIMIT_WINDOW_MIN=15

# URL du microservice de vérification de diplômes (diploma-verifier)
DIPLOMA_VERIFIER_URL=http://localhost:8000
