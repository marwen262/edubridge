# Framework web
fastapi==0.111.0
uvicorn[standard]==0.29.0
python-multipart==0.0.9

# OCR
pytesseract==0.3.10

# Vision / Image
opencv-python-headless==4.9.0.80
scikit-image==0.23.2
Pillow==10.3.0
numpy==1.26.4

# NLP
spacy==3.7.5
langdetect==1.0.9

# Validation
pydantic==2.7.1

# Utilitaires
# puremagic = pure-Python MIME detection (no C bindings, cross-platform).
# Remplace python-magic/libmagic qui posait des problèmes de chargement
# dans le worker FastAPI (cf. issue #1, V7 Phase 2).
puremagic==1.28
aiofiles==23.2.1

# QR code detection (requires libzbar native library)
pyzbar==0.1.9

# Google Vision API OCR fallback (optional — needs GOOGLE_APPLICATION_CREDENTIALS)
google-cloud-vision==3.7.2

# EasyOCR — local Arabic OCR fallback for bottom-crop name extraction (no API key)
easyocr==1.7.2

# MantraNet / deep-learning manipulation detector
# Install CPU build separately (not via PyPI index):
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
torch>=2.12.0
torchvision>=0.27.0

# Tests
pytest==8.2.0
pytest-asyncio==0.23.6
httpx==0.27.0
